# HTTPS 与反向代理验证记录（2026 年 9 月 17 日）

## 验证对象

- 实现提交：`d5766f4 feat: add local HTTPS reverse proxy boundary`
- 设计说明：`D:\MeetingMind\docs\HTTPS与反向代理.md`
- 本地配置：`D:\MeetingMind\deploy\Caddyfile.local`
- 生产示例：`D:\MeetingMind\deploy\Caddyfile.production.example`
- 自动检查报告：`D:\MeetingMind\data\release-check\phase8-https-edge-final-2026-09-17.json`

## 运行状态

- FastAPI：仅监听 `127.0.0.1:8000`；
- Caddy：Docker `edge` profile，宿主机只绑定 `127.0.0.1:8080` 和 `127.0.0.1:8443`；
- HTTPS：`https://localhost:8443`；
- HTTP：`http://localhost:8080`；
- Redis/RQ：1 个 Worker 在线，队列为 0。

本轮没有把 Caddy 内部 CA 根证书安装进 Windows 或浏览器信任库，避免未经明确授权改变系统信任配置。自动验证使用 `curl -k` 检查实际 TLS 握手。

## 运行态验证

| 场景 | 结果 |
| --- | --- |
| `GET https://localhost:8443/api/v1/health` | `200`，返回 `phase-8` |
| `GET http://localhost:8080/` | `308`，跳转到 `https://localhost:8443/` |
| HTTPS 静态前端 `/` | `200` |
| HTTPS `/api/v1/meetings` 无令牌 | `401` |
| HTTPS `/api/v1/meetings` 正确令牌 | `200` |
| FastAPI 直接响应 | 包含 `Cache-Control: no-store` 和 `X-Content-Type-Options: nosniff` |

HTTPS 响应已确认包含：

- `Content-Security-Policy`；
- `X-Content-Type-Options: nosniff`；
- `X-Frame-Options: DENY`；
- `Referrer-Policy: no-referrer`；
- 限制摄像头、麦克风和定位的 `Permissions-Policy`。

## 配置验证

- Docker Compose `edge` profile：通过；
- `Caddyfile.local`：固定版本 `caddy:2.10.2-alpine` 解析通过；
- `Caddyfile.production.example`：同版本解析通过；
- 本地配置显式使用 `tls internal`；
- 生产示例明确禁止内部 CA，包含 HSTS，并默认代理到 `127.0.0.1:8000`；
- Caddy 请求体上限为 210 MB，后端默认音频上限仍为 200 MB；
- Caddy Docker 日志限制为 10 MB × 5 个文件。

## 完整发布检查

在干净提交 `d5766f4` 上执行：

```powershell
.\scripts\pre_release_check.ps1 -ReportPath .\data\release-check\phase8-https-edge-final-2026-09-17.json
```

结果：

- HTTPS/Caddy 本地与生产配置校验：通过；
- 本地 HTTPS 反向代理与安全响应头：通过；
- 后端 pytest：`57 passed`；
- 前端 Vitest：`18 passed`；
- 前端类型检查与生产构建：通过；
- API、MySQL、Redis、RQ Worker、Alembic、OpenAPI：通过；
- repository audit：`0 issues`；
- Git 工作区：干净；
- 汇总：`0 failures`、`0 warnings`。

## 边界结论

本机已经形成实际 TLS、静态前端、API 反向代理、Bearer 访问控制和安全响应头闭环，且默认不向局域网开放。

这不等于公网生产验收。真实部署仍需使用实际域名和可信 CA，验证证书续期、防火墙、安全组、DNS、代理告警和正式用户身份系统。

下一项按计划进入 MySQL 与 `data/meetings` 的一致性备份、隔离恢复和恢复演练。
