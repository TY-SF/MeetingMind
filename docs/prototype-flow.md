# MeetingMind 当前页面流程

```text
会议列表
  → 新建会议（真实 multipart 音频上传）
  → 处理进度（轮询真实任务状态）
  → 会议详情（只读转录）
  → 手动触发 AI 分析
  → 审核摘要、结论和待办
  → 带 version 的整体保存 / Markdown 导出 / 永久删除
```

前端不再使用 localStorage 模拟会议或固定计时器。录音在本机保存并由 WhisperX 转录；用户点击“生成 AI 分析”后，仅带时间戳的转录文本会发送到已在后端配置的模型服务。AI 输出始终作为可编辑草稿，人工保存时使用乐观锁防止覆盖。
