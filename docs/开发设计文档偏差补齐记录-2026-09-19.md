# MeetingMind 开发设计文档 A–E 偏差补齐记录

**完成日期：2026 年 9 月 19 日｜实现提交：`540995f3b586f5c84bb39c296297703e5f7fee5a`**

本记录用于证明此前识别的 A–E 五项偏差或证据缺口已经关闭。记录不包含令牌、密码、会议正文、完整转录或模型原始响应。

## A. 阶段级断点恢复

音频流水线不再笼统地从原始音频全部重跑，而是保存并验证以下原子检查点：

- `working/preprocessing.checkpoint.json`：规范化音频时长与 SHA-256；
- `working/transcribing.checkpoint.json`：WhisperX 原始转录阶段产物；
- `working/aligning.checkpoint.json`：WhisperX 对齐阶段产物；
- `results/transcript.json`：最终成功产物。

每个检查点绑定源文件 SHA-256、模型、语言、说话人分离配置和预计说话人数。写入使用临时文件后原子替换；损坏、版本不符、源文件不符或配置不符的检查点不会被复用。恢复会从最近一个完整有效检查点继续，缺少有效检查点时才回退到最早必要阶段。

自动测试 `backend/tests/test_audio_stage_checkpoints.py` 验证：

1. 对齐阶段中断后，恢复不会重复执行规范化和转录；
2. 源文件签名变化后，旧检查点会被拒绝并重新执行必要阶段。

## B. README 状态一致性

`README.md`、`README.zh-CN.md` 和 `backend/README.md` 已同步：

- 删除“尚未取得 GitHub-hosted 绿色运行”的过时表述；
- 更新为当前 GitHub Actions 门禁范围；
- 更新阶段恢复语义；
- 更新后端测试数量为 `73 passed`；
- 增加动态 GitHub Actions 状态徽章；
- 将本地安装和启动命令切换为锁定的 uv 工作流。

## C. uv 与 pyproject.toml

仓库根目录新增：

- `pyproject.toml`：Python 3.12、运行依赖、开发依赖和可选 WhisperX 依赖；
- `uv.lock`：完整锁定依赖图。

GitHub Actions 使用固定版本 `uv==0.12.17` 执行 `uv sync --frozen --group dev`，随后运行依赖一致性检查、测试和验收脚本。CI 使用根目录 `.venv`；本机 PowerShell 运维入口通过 `scripts/resolve_runtime_venv.ps1` 选择完整运行环境，并优先选择带 CUDA 的 WhisperX/pyannote 环境，避免基础环境遮蔽已验证的 GPU 环境。

## D. 完整 Git 历史敏感信息审计

新增 `scripts/git_history_audit.py`：

- 枚举全部 Git 历史对象；
- 对历史文本 blob 执行 OpenAI、Hugging Face、GitHub、AWS、私钥和带凭据服务 URL 的高置信度规则扫描；
- 不输出匹配到的秘密值；
- 检查超限历史 blob；
- 已加入本地发布检查和 GitHub Actions。

CI 后端 checkout 使用 `fetch-depth: 0`，避免浅克隆把“当前提交扫描”误称为“完整历史扫描”。Run #7 实际检查 `337` 个历史文本 blob，结果为 `0 issues`。

## E. GitHub 仓库名称

GitHub 仓库显示名称已从 `TY-SF/MeetingMind` 调整为设计文档要求的：

```text
TY-SF/meetingmind
```

本地 `origin` 同步为：

```text
https://github.com/TY-SF/meetingmind.git
```

## 自动化复验

实现提交对应的 [GitHub Actions Run #7](https://github.com/TY-SF/meetingmind/actions/runs/35425917841) 为 `success`：

- [Backend tests (Python 3.12)](https://github.com/TY-SF/meetingmind/actions/runs/35425917841/job/105851707826)：成功；
- [Frontend type-check and build (Node 22)](https://github.com/TY-SF/meetingmind/actions/runs/35425917841/job/105851708066)：成功；
- 后端：`73 passed`；
- 前端：`18 passed`，类型检查和生产构建通过；
- 当前候选文件审计：`0 issues`；
- 完整 Git 历史审计：`337` 个文本 blob，`0 issues`；
- 三组 Gold Standard 指标全部为 `1.0`；
- Day-7 端到端证据校验通过；
- 制品 `day7-acceptance-evidence` 已生成且未过期。

## 判定

A–E 五项偏差或证据缺口均已关闭。生产域名、正式多人身份、通知渠道和长期备份治理仍属于后续生产化工作，不属于本记录的 A–E 范围，也不影响第一周及第七天验收结论。
