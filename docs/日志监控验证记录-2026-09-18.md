# 日志、监控与本地告警验证记录

**日期：2026 年 9 月 18 日**

## 自动化验证

- 后端完整测试：`70 passed`；
- 日志遮盖测试：Bearer Token、API Key、密码、URL 凭据和嵌套敏感字段不会写入 JSON；
- 日志轮转测试：活动文件和历史文件数量有界，每行保持有效 JSON；
- 监控测试：不可信外部域名不能跳过 TLS 校验；报告不包含访问令牌；告警只在非健康状态变化时追加；
- PowerShell 语法：API/Worker 后台启动、运行监控和发布检查脚本通过；
- Docker Compose：MySQL、Redis、Caddy 日志轮转配置解析通过。

## 真实运行态

- API 后台启动成功，`data/logs/api.jsonl` 已生成；
- RQ Worker 在线，`data/logs/worker.jsonl` 已生成；
- 本机 HTTPS 运行检查结果：
  - API 存活：通过；
  - MySQL 就绪：通过；
  - Redis/RQ Worker：通过，1 个 Worker 在线；
  - 数据盘空间：通过；
  - 结构化日志：通过；
  - 备份时效：通过，存在新鲜且已完成隔离恢复验证的归档。

监控总状态为 `healthy`，报告不输出会议内容或任何密钥。

## 未完成的外部边界

- 尚未把本地告警事件接入目标组织的通知渠道；
- 尚未定义生产值班负责人、响应时限和升级路径；
- 完整数据备份阻塞已解决；仍需在目标环境配置归档加密、异地保存和删除生命周期。

因此本项完成的是“有界日志 + 本地机器可读监控 + 状态变化告警事件”，不是完整生产监控平台。
