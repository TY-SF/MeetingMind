# 第七阶段开发说明：发布级验收与质量加固

**开始日期：2026 年 9 月 13 日**

## 阶段目标

第六阶段已经完成核心异步处理能力。第七阶段不再盲目增加业务功能，而是从“用户上传一段音频后，系统是否可观察、可恢复、可验证、可发布”出发，把现有能力固化为可重复的发布门禁。

本阶段采用以下判定原则：

1. 运行状态必须有明确证据，不能仅凭进程窗口判断服务正常。
2. 队列不可用时不能伪装成已排队；Worker 缺失时必须可诊断。
3. 失败、中断、重试和删除必须保持数据库、本地文件与队列状态一致。
4. 自动检查必须可重复执行，并且不能打印密钥、Token、密码、完整转录或模型响应。
5. 文档中的数字必须注明检查日期；历史结果不能冒充当前结果。

## 第一批已完成工作

### 1. 修复发布前检查脚本

`scripts/pre_release_check.ps1` 原先存在 PowerShell 语法错误：OpenAPI 路径数组中混入了字面量 `` `n ``，导致脚本在真正检查前无法解析。现已修复，并增加：

- Docker Compose 配置校验；兼容 `docker-compose` 独立命令和 `docker compose` 插件；
- Redis/RQ Worker 健康检查，要求 Redis 为 `ok`、Worker 在线且队列状态为 `ready`；
- `-SkipInfrastructure`、`-SkipLiveServer`、`-SkipFrontend` 明确跳过开关；
- `-ReportPath` 生成不含敏感值的 JSON 检查报告；
- OpenAPI 重试接口路径校验恢复正常。

### 2. 当前可重复检查命令

```powershell
cd D:\MeetingMind
.\scripts\pre_release_check.ps1 -ReportPath .\data
elease-check\latest.json
```

只在服务尚未启动时进行代码、迁移、依赖和 Gold Standard 检查：

```powershell
.\scripts\pre_release_check.ps1 -SkipLiveServer -ReportPath .\data
elease-check\offline.json
```

只在没有 Docker 的环境中明确跳过 Compose 校验：

```powershell
.\scripts\pre_release_check.ps1 -SkipInfrastructure -SkipLiveServer
```

## 当前验证结果（2026 年 9 月 13 日）

- PowerShell 脚本语法：通过；
- Docker Compose 配置校验：通过；
- Alembic：`0007_one_job_per_meeting (head)`；
- 后端 pytest：47 passed；
- Gold Standard：3 组受控样本，所有当前指标 1.0；
- `pip check`：通过；
- repository audit：0 issues；
- 运行中 API、Redis/RQ Worker 和前端全套检查：需在服务启动后执行，不能用离线结果替代。

## 第二批工作清单

1. 启动 API 与 Worker，执行包含 `/health`、`/health/ready`、`/health/queue`、OpenAPI 的完整发布检查；
2. 验证 Redis 不可用时上传返回 `503 QUEUE_UNAVAILABLE`；
3. 验证 Worker 中断后任务进入 `FAILED`，阶段事件为 `INTERRUPTED`，重试不生成重复业务任务；
4. 在 Docker MySQL + Redis 环境执行一次迁移和受控链路验证；
5. 记录一份带日期、提交版本和命令输出摘要的发布检查记录；
6. 统一 README、中文 README 与历史发布记录中的测试数字和阶段说明。

## 非目标

本阶段不引入账户权限、实时转录、任务取消、多人协作或公网部署。这些能力仍属于设计文档中的后续 Roadmap。
