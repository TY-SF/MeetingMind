# 隐私与数据边界验证记录（2026 年 9 月 17 日）

## 验证对象

- 隐私边界实现提交：`f9b54e0 feat: require explicit recording and AI data consent`
- 权威说明：`D:\MeetingMind\docs\隐私与数据边界.md`
- 自动检查报告：`D:\MeetingMind\data\release-check\phase7-privacy-final-2026-09-17.json`

## 验证结论

从“未经明确确认的数据不应被保存或外发”这一基本约束出发，本轮确认了两层门禁：

1. 上传录音前必须确认处理授权、本地处理边界和无自动脱敏；
2. 每次生成或重新生成 AI 草稿前必须再次确认转录相关数据适合发送到已配置的模型服务。

运行中 OpenAPI 已包含：

- `POST /api/v1/meetings` 的 `data_processing_confirmed`；
- `POST /api/v1/meetings/{meeting_id}/analysis` 的 `analysis_data_confirmed`。

前端上传页提供必选确认框；首次 AI 分析和重新生成都会显示最终发送确认，并明确原始音频不会发送、系统不会自动脱敏。后端同时执行相同门禁，因此直接调用 API 也不能绕过。

## 运行态负向验证

在本机运行中的 API 上执行未确认请求：

| 场景 | 结果 |
| --- | --- |
| 上传请求缺少 `data_processing_confirmed` | `422 DATA_PROCESSING_CONFIRMATION_REQUIRED` |
| AI 分析请求缺少 `analysis_data_confirmed` | `422 ANALYSIS_DATA_CONFIRMATION_REQUIRED` |
| 验证前后会议记录数量 | `1 → 1`，未新增会议 |

两项检查均在保存文件或调用模型服务之前被拒绝，没有生成新的会议记录。

## 自动检查结果

- 后端 pytest：`53 passed`；
- 前端 Vitest：`16 passed`；
- 前端类型检查与生产构建：通过；
- Alembic：`0007_one_job_per_meeting (head)`；
- API、MySQL、Redis、RQ Worker 与运行中 OpenAPI：通过；
- Python 依赖一致性、Docker Compose、受控 Gold Standard：通过；
- repository audit：`0 issues`；
- 发布检查：`0 failures`、`0 warnings`，检查时 Git 工作区干净。

## 仍然存在的边界

- 当前版本没有账户、认证、角色权限或租户隔离，只适合受信任的本机或受控内网环境；
- 系统不会自动脱敏，使用者必须在 AI 分析前自行审查转录文本和会议背景；
- 自定义 `LLM_BASE_URL` 可能指向第三方服务，其保留、训练、地域和合规政策需要部署者单独核验；
- 操作系统备份、数据库备份、导出文件和手工副本不由会议删除操作自动清理。
