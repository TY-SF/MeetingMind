# Empty by default: Vite proxies /api to http://127.0.0.1:8000 in development.
# Set this only when the backend is exposed on a different origin.
VITE_API_BASE_URL=

# Optional Vite development proxy target.
MEETINGMIND_BACKEND_URL=http://127.0.0.1:8000
